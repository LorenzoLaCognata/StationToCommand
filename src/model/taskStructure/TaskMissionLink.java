package model.taskStructure;

import model.missionStructure.Mission;
import model.missionStructure.MissionLink;

public class TaskMissionLink extends MissionLink {

    public TaskMissionLink(Mission mission) {
        super(mission);
    }

}