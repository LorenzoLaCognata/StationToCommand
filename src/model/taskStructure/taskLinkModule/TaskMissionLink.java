package model.taskStructure.taskLinkModule;

import model.linkStructure.missionLinkModule.MissionLink;
import model.missionStructure.missionModule.Mission;

public class TaskMissionLink extends MissionLink {

    public TaskMissionLink(Mission mission) {
        super(mission);
    }

}